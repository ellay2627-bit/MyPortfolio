# OSS 最低成本发布说明

这个方案只服务公开前台站点：
- 你的 `admin` 继续只在本地使用
- 公开站从 `dist-oss/` 上传到阿里云 OSS
- 作品图片继续复用你现有的阿里云 OSS 外链

## 你以后更新作品的流程

1. 本地进入后台，新增或修改作品并发布
2. 在项目根目录运行：

```bash
npm run build:oss
```

3. 命令完成后，会生成：

```bash
dist-oss/
```

4. 把 `dist-oss/` 里的内容上传到你的 OSS bucket 根目录

## dist-oss 里包含什么

- 首页 `index.html`
- 作品详情页 `works/<id>/index.html`
- Next 前端静态资源 `/_next/static/*`
- 作品静态数据 `/static/*.json`
- PDF 页面 `/pdf/*`

不包含：
- 本地 admin 后台
- 本地草稿和内容源文件
- 作品图片二次拷贝

## 你需要准备给我的信息

如果你想让我继续帮你把“上传”这一步也串起来，你需要提供：

1. 你的 OSS bucket 名称
2. OSS 所在地域
3. 你打算绑定的公开域名
4. 你是否已经开通 OSS 静态网站托管

## 成本控制建议

- 最低成本先不要接 CDN
- 先让公开站直接走 OSS 静态网站托管
- 如果后面访问量上来，再按需接 CDN

## 注意

如果你要用自定义域名给中国大陆用户公开访问，通常还需要：
- 域名备案
- OSS 静态网站托管配置

如果只是先测试速度，可以先用 OSS 提供的访问地址验证。
