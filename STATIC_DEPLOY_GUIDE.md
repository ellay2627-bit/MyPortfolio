# 静态化部署完整指南

## 概述

本项目采用**完全静态化**的部署方案，确保：
- 前台100%静态，无API依赖
- 后台发布自动生成静态数据文件
- 部署速度快，CDN友好
- 数据同步机制完善

## 架构说明

### 数据流向

```
后台管理 → 草稿保存 → 发布操作 → 生成静态数据文件 → Git提交 → 自动部署 → 前台展示
                                 ↓
                          content/works/data.json (静态文件)
```

### 关键文件

- **前台数据读取**: `src/components/Work.tsx` - 通过动态导入读取 `content/works/data.json`
- **后台发布API**: `src/app/api/works/route.ts` - 负责生成静态数据文件
- **静态数据文件**: `content/works/data.json` - 前台使用的静态数据源

## 工作流程

### 1. 内容发布流程

1. 进入后台管理：`/admin`
2. 编辑/添加作品内容
3. 点击「发布更新」按钮
4. 系统自动：
   - 将草稿内容复制到发布目录
   - 生成统一的 `data.json` 文件
   - 触发前台数据更新事件
5. 前台实时（无需刷新）或下次刷新时显示最新内容

### 2. 部署到生产环境

#### 方式A：Git+Vercel自动部署（推荐）

1. **在本地完成内容发布**
   ```bash
   # 在后台发布作品后
   git status
   git add content/works/
   git commit -m "update: 发布最新作品"
   git push
   ```

2. **Vercel自动部署**
   - 推送到GitHub后，Vercel自动检测到变更
   - 自动触发构建和部署
   - 部署完成后，CDN自动更新

3. **部署验证**
   - 访问你的Vercel域名
   - 确认作品内容已更新

#### 方式B：手动部署

```bash
# 1. 构建生产版本
npm run build

# 2. 部署到你的服务器
# 将 .next 或 out 目录上传到服务器
```

## 数据同步机制

### 前后台同步保证

1. **实时事件通知**
   - 后台发布时触发自定义事件 `portfolio_publish`
   - 同时更新 `localStorage` 中的 `publish_timestamp`
   - 前台监听这些事件，自动刷新数据

2. **本地缓存策略**
   - 前台使用 `localStorage` 缓存作品列表数据
   - 缓存有效期：24小时
   - 发布时自动清除旧缓存
   - 优先从缓存加载，后台刷新最新数据

3. **数据一致性**
   - 后台和前台使用同一数据源：`content/works/data.json`
   - 发布时重新生成此文件，确保数据一致

## 优化的加载策略

### 1. 分阶段加载

```
首屏加载 → Hero组件（立即显示）
          ↓
        About组件（懒加载）
          ↓
        作品列表（懒加载+骨架屏）
          ↓
        其他组件（懒加载）
```

### 2. 作品数据加载策略

- **阶段1**: 快速加载作品列表基本信息（标题、封面、分类）
- **阶段2**: 后台异步加载完整作品数据（用于详情页）
- **按需加载**: 点击作品卡片时才加载该作品的完整媒体内容

### 3. 骨架屏优化

- 作品列表骨架屏：6个，与实际作品数量一致
- 详情页媒体骨架屏：根据内容动态显示
- 平滑的加载过渡动画

## 缓存体系

### 1. 浏览器缓存（localStorage）

| 缓存键 | 内容 | 有效期 |
|--------|------|--------|
| `portfolio_works` | 作品列表数据 | 24小时 |
| `portfolio_works_detail` | 作品详情缓存 | 24小时 |
| `publish_timestamp` | 最后发布时间戳 | 永久 |
| `portfolio_detail_state` | 详情页状态 | 会话 |

### 2. 服务端缓存（API层）

- API响应缓存5分钟
- 发布操作自动清除缓存
- 内存缓存，重启后重置

### 3. CDN缓存（生产环境）

- 静态资源长期缓存
- 数据文件使用版本化或时间戳
- Vercel自动处理缓存失效

## 性能优化建议

### 1. 图片优化

- 使用WebP格式
- 实现响应式图片
- 添加懒加载
- 图片压缩（<500KB）

### 2. 代码分割

- 已实现组件级懒加载
- 路由级代码分割
- 第三方库按需加载

### 3. 预加载策略

```javascript
// 在Work组件中已实现
// 页面加载时预加载数据
// 用户点击时立即响应
```

## 监控和调试

### 1. 浏览器控制台日志

打开控制台查看：
- `开始初始化数据...`
- `从缓存快速加载完成`
- `静态数据加载完成，已更新到最新数据`
- `收到发布事件，更新数据`

### 2. 验证数据同步

1. 在后台发布新作品
2. 观察前台是否显示「作品数据已更新」提示
3. 检查 `content/works/data.json` 是否更新
4. 验证前台作品列表是否更新

## 常见问题

### Q: 发布后前台不更新？

A: 检查以下几点：
1. 确认点击了「发布更新」按钮
2. 查看浏览器控制台是否有错误
3. 检查 `content/works/data.json` 文件是否更新
4. 尝试硬刷新（Ctrl+Shift+R）

### Q: 部署到Vercel后数据没变？

A: 确保：
1. 已将 `content/works/` 目录提交到Git
2. Vercel构建日志显示成功
3. CDN缓存已失效（等待几分钟或手动清除）

### Q: 如何确认数据已静态化？

A: 检查：
1. `content/works/data.json` 文件存在且包含最新数据
2. 前台代码中没有直接调用API获取作品列表
3. 构建产物中包含此JSON文件

## 最佳实践清单

- [ ] 每次内容变更后先在后台发布
- [ ] 发布后确认 `data.json` 已更新
- [ ] Git提交时包含 `content/works/` 目录
- [ ] 定期检查图片大小和格式
- [ ] 监控部署日志和性能指标
- [ ] 测试前后台数据同步
- [ ] 验证移动端和桌面端都正常

## 技术支持

如遇问题，检查：
1. 浏览器控制台错误
2. 服务器日志（Vercel）
3. Git提交记录
4. 文件权限（本地开发）
