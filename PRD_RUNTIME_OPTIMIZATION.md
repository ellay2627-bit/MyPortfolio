# 作品集运行优化补充 PRD

本文件用于补充 [PRD.md](/Users/ellay/Documents/Portfolio/PRD.md)，描述当前项目已经落地的运行时、静态资源和发布链路优化方案。旧版 PRD 更偏页面与功能描述，未覆盖当前的加载、压缩、缓存与 Vercel 免费额度约束。

## 1. 优化目标

- 不改现有前台视觉、动画、交互
- 不改后台操作路径与使用方式
- 降低静态资源体积与发布波动
- 降低首页首屏和详情弹窗的数据加载压力
- 适配 Vercel 免费版对静态资源、构建与原点传输的限制

## 2. 当前资源策略

### 2.1 封面图策略

- 首页作品封面保留在本地 `public/images/works`
- 封面用于首页卡片和详情入口，优先保证命中本地静态资源
- 保存作品时，封面图片优先经过 TinyPNG 压缩，保持分辨率不变

### 2.2 详情媒体策略

- 普通详情图片默认仍保留本地
- 图片在保存时先经过 TinyPNG 压缩
- 当前默认控制策略：
  - 后续新增图片尽量小于 `2MB`
  - 超大图片与大 GIF 作为人工控制项处理
- 视频仍保留当前结构兼容能力，但不作为默认本地大资源推荐方案

### 2.3 未来扩展策略

- 已预留第三方托管接入能力
- 若后续本地大图/视频继续增长，可启用远端托管
- 当前阶段先不启用图床，避免增加维护成本

## 3. 数据加载策略

### 3.1 首页列表

- 首页只读取 `public/static/works-list.json`
- 列表数据只保留展示首页所需字段：
  - `id`
  - `title`
  - `brief`
  - `category`
  - `cover`
  - `ratio`
  - `order`
- 不再为首页预加载整份作品详情数据

### 3.2 详情弹窗

- 详情弹窗按需读取单个 `public/static/work-{id}.json`
- 用户点击某个作品时才请求该作品的详情 JSON
- 上一条/下一条导航只依赖列表顺序，不依赖全量详情预加载

### 3.3 缓存策略

- 首页列表数据支持浏览器本地缓存
- 单个作品详情支持按作品维度缓存
- 后台发布后，通过事件和本地缓存清理机制通知前台刷新
- 详情缓存按单作品键清理，而不是全量刷新页面

## 4. 发布与静态生成策略

### 4.1 发布模型

- 后台仍通过“保存草稿 -> 发布更新”的原流程工作
- 发布时不再采用“先清空整个目录再重建”的重型策略
- 当前发布策略为：
  - 覆盖更新当前草稿对应作品
  - 清理已不存在的旧作品文件
  - 重新生成静态列表和单作品详情文件

### 4.2 静态文件产物

- `content/works/data.json`
  - 发布后的完整作品数据快照
- `public/static/works-list.json`
  - 首页列表专用数据
- `public/static/work-{id}.json`
  - 单个作品详情数据

### 4.3 文件清理策略

- 删除作品时：
  - 删除对应作品 JSON
  - 删除对应静态详情 JSON
  - 删除对应本地封面和媒体文件
- 静态生成脚本会清理不再被任何作品引用的孤儿文件

## 5. 压缩策略

### 5.1 图片压缩

- 保存作品时，服务端对 Base64 图片进行 TinyPNG 压缩
- 压缩目标是“保持尺寸不变，尽量降低体积”
- 超过一定体积的资源不强制走 TinyPNG，以降低大文件请求失败风险

### 5.2 人工上传建议

- 封面图建议控制在 `500KB - 1.5MB`
- 详情普通图片建议控制在 `2MB` 以内
- 动图优先减少：
  - 帧数
  - 时长
  - 分辨率
- 若素材为长视频，优先使用视频链接或后续远端托管
- 不再建议反复对全量历史图片运行批量压缩脚本，避免持续消耗 TinyPNG 配额
- 批量压缩脚本仅适合一次性整理历史资源，后续以“新增少量图片自动压缩 + 人工控制素材体积”为主

## 6. Vercel 额度优化点

- 首页不再拉取整份详情数据
- 详情页改为按需单文件加载
- 静态资源文件变化面缩小，减少无效构建波动
- 发布逻辑更偏增量，减少全量重写
- `.gitignore` 与 `.vercelignore` 已补充，减少无效文件进入部署链路

## 7. 当前结论

当前项目的最优默认策略不是“全面图床化”，而是：

- 本地静态资源为主
- 保存时做压缩
- 列表与详情拆分加载
- 发布走更轻量的静态增量生成
- 人工控制大图体积，优先把新增内容压到 `2MB` 以下

这套方案更符合当前项目阶段，也更适合低维护成本地运行在 Vercel 免费版上。
